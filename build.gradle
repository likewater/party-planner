apply plugin: 'java'
apply plugin: 'application'

archivesBaseName = "party-planner"
version = '1.0'
mainClassName = "PartyPlanner"

repositories {
  mavenCentral()
}

dependencies {
  testCompile group: 'junit', name: 'junit', version: '4.+'
}
